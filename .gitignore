# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (never commit secrets)
.env
.env.local
.env.production
.env*.local

# Next.js build output
.next/
next/.next/

# Build artifacts and tarballs
*.tar
*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.jks
*.keystore
*.b64
keystore.properties

# Credentials and service-account JSONs — never commit. These are
# delivered to CI through GitHub Secrets (FCM_SERVICE_ACCOUNT_JSON,
# GOOGLE_SERVICES_JSON) and to local dev machines out-of-band.
google-services.json
*firebase-adminsdk*.json
gen-lang-client-*.json